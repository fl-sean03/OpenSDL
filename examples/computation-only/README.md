# Computation-only laboratory

```bash
uv run --locked opensdl run examples/computation-only/workflow.yaml \
  --manifest examples/computation-only/opensdl.yaml \
  --inputs '{"x":2,"a":1,"b":0,"c":-1}'
```
