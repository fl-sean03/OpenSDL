# {{ name }} adapter

The generated package exposes a passing simulator-first implementation of
`{{ capability_id }}`. Replace the generic schemas and echo behavior with the
actual transport, typed errors, lifecycle, simulator, and validation evidence.
