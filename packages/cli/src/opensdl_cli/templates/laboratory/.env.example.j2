OPENSDL_MANIFEST=opensdl.yaml

# Every ${env:NAME} written in opensdl.yaml must be set here or in the environment. A reference that
# does not resolve is an error naming the variable, so a missing credential fails when the manifest
# loads rather than at an instrument. Copy this file to .env, which is ignored by Git.
#
# BALANCE_TOKEN=
