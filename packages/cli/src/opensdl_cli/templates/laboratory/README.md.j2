# {{ name }}

Organization-owned laboratory implementation built on OpenSDL.

## Start locally

OpenSDL alpha packages must be available from a configured registry. For a local smoke test before
registry publication, build a wheelhouse in the OpenSDL framework checkout and pass it to `uv`:

```bash
uv sync --find-links /path/to/OpenSDL/dist
uv run opensdl validate opensdl.yaml \
  --workflow workflows/first-run.yaml \
  --workflow workflows/manual-check.yaml
uv run opensdl doctor --manifest opensdl.yaml
uv run opensdl run workflows/first-run.yaml \
  --manifest opensdl.yaml \
  --inputs '{"red_fraction":0.5,"blue_fraction":0.5,"sample_id":"demo","total_mass_g":5}'
```

The local wheelhouse path can be recorded in `uv.lock`. Do not treat that lock as portable across
clones. CI always validates the repository agent contract; its full job remains visibly skipped
until the packages resolve from a stable source and the repository variable
`OPENSDL_PACKAGES_AVAILABLE` is set to `true`.

The initial profile is simulator-only. Replace or extend adapters, capabilities,
policies, domain models, workflows, and deployments for this organization.

Repository procedures live in `.agents/skills/`. Claude Code reads the same skills through
`.claude/skills/` compatibility adapters.
